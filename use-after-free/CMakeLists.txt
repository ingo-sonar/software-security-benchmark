process_all_files()

# file(GLOB UAF_SRC RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.c *.cpp)

# foreach(file ${UAF_SRC})
#     generate_ll_file(FILE ${file} TBAA)
#     get_filename_component(test ${file} NAME_WE)
#     add_executable(${test} ${file})
# endforeach()
